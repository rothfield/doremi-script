  if save_to_samples 
    Dir.chdir("public/compositions") do
      ary=Dir.glob("#{fname}*") 
    end
    puts "ary is #{ary.inspect}"
    ary.each do |x| 
     puts "x is #{x}"
     x =~ /^.*\.(.*)$/
     suffix=$1
     fp= "#{comp}/#{x}"
    `cp #{fp} ../../samples/#{simple_file_name}.#{suffix}`
    end
  end
